// WiFi credentials for the UI dev tools (esp32dev_uidev env only).
// Copy to include/secrets.h (gitignored) and fill in. Without it the dev firmware
// still boots and answers serial STATUS, but starts no web server.
#pragma once

#define WIFI_SSID "your-network"
#define WIFI_PASSWORD "your-password"
